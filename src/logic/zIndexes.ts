
export default {
    initial: 0,

    // 帖的头部
    postHead: 10000,

    // 引用视图，否则超出其父视图外部的部分内容会被裁剪掉
    refPost: 20000,

    // 折叠时的遮盖
    overflowMask: 30000,

    // fixed
    fixedWrapper: 40000,
}