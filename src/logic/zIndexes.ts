
export default {

    // 负数 zIndex，如 `-n` 与第 n 页对应

    initial: 0,

    // 帖。
    // 如果引用视图没有，超出其父视图外部的部分内容会被裁剪掉？
    post: 2_000_000,

    // 折叠时的遮盖
    overflowMask: 1_900_000,

    // 帖的头部
    postHead: 3_200_000,

    // fixed
    fixedWrapper: 4_000_000,
}